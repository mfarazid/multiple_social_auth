.col-lg-12
  - if current_user
    %p{:id => 'signed'}Sign in as #{current_user.name} Not you? #{link_to "sign out", signout_path, id: 'sign_out', :class => 'btn btn-xs  btn-info'}
  %article.jumbotron
    %h2 Welcome to Multiple Social Auth Application!
    %p This is a sample application to login with Facebook, Twitter, and Google.
